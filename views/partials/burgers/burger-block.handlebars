<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}DEVOUR TIME!{{else}}REGURGITATE!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">CREMATE!</button>
</li>
