services:
  - type: web
    name: catburger
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false   # Set this manually in the Render dashboard
      - key: NODE_ENV
        value: production
